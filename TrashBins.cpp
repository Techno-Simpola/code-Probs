#include<bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
	#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	#endif

	int n,t;cin>>t;
	char ch;
	vector<string> s;
	while(t--)
	{
		cin>>n;
		for(auto i = 0; i < n ; i++)
		{
			cin>>ch;
			v.push_back(ch);
		}
		int flag=0;
		for(auto i=0;i<n;i++)
		{
			if(v[0]!='0')
			{
				if(v[i]=='0')
				{
					flag = 
				}	
			}
			

		}	
	}

	return 0;
}