#include<bits/stdc++.h>
using namespace std;
//we need to leatn stack data structure for this problem
int main()
{
	#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	#endif

	int t;cin>>t;
	string s;
	while(t--)
	{
		cin>>s;
	}
	return 0;
}